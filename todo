labels/gotos

Cast to bool: Bit-and with itself and then TRF C -> A
maybe?

Optimisation: copy out a single calculation of address when array is used a lot

BUG: Writing to a struct pointer doesn't write each field.
     It used to do *something*, but now it just crashes

Separate out the repeated operator function call code in codegen

Assign struct to any non named pointer

Optimise statement: Remove add/sub/mul 0 and mul/div 1

Don't allow names beginning with "__"
